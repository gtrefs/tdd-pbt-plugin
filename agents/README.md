# agents/

Agent definition files for TDD and PBT workflow phases.

Populated by task 02 (migrate-agents). Each `.md` file here defines a
specialized sub-agent (red, green, refactor, test-list, property-list,
find-properties, implement-properties, property-red, property-green).
